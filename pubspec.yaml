name: velitt
description: Velitt Authentication App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.7
  provider: ^6.0.5
  supabase_flutter: ^2.8.3
  fl_chart: ^0.70.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/coupons/
    - assets/icons/
  # fonts:
    # - family: Velitt
      # fonts:
        # - asset: assets/fonts/Velitt-Regular.ttf